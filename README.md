# Discord 文件仓库 Bot（Repo Bot）

一个以 **Discord 本身作为存储后端** 的文件仓库机器人：成员可以上传任意类型的文件，
所有文件统一归档到 **当前服务器的专用子区** 或 **独立存储服务器**（由管理员设置），
并且内置 **下载审计机制** —— 每一次下载都会记录「谁、在什么时间、下载了哪个文件」。

## ✨ 功能特性

- 📤 **任意文件上传**：通过 `/upload` 上传任何附件，自动生成唯一文件 ID
- 🗂️ **两种存储模式**（管理员通过 `/storage_mode` 切换）：
  - **子区模式（category）**：自动在当前服务器创建 `📁 文件仓库` 分类 + 存储频道，
    也可以通过 `category` 参数**直接使用你已建好的子区**
  - **独立服务器模式（guild）**：将文件存储到另一个专门的 Discord 服务器
- 🔢 **文件编号**：每个文件自动分配服务器内编号（#1、#2……），
  `/download`、`/fileinfo`、`/history`、`/delete` 均可直接输入编号操作
- 🗳️ **一键整理**：`/organize` 预扫描后发起管理员投票（范围可选当前频道 / 当前子区 /
  整个服务器，默认需 1 / 2 / 3 名管理员同意，可用 `/vote_config` 调整，发起者自动计 1 票），
  自动迁移旧位置文件，并**扫描范围内频道里未登记的散落文件**（成员直接发的），登记入库后归拢到存储频道；
  **投票通过后进入范围确认面板**：选择「黑名单（整理所选以外）/ 白名单（只整理所选）」
  模式 + 名单频道，可开关「散落文件登记」（关闭后仅迁移已登记文件），
  还可一键保存为默认配置；执行中可用 `/organize_cancel` 强制取消
- ⛔ **下载风控**：`/risk_config` 配置「时间窗口内下载次数上限 + 封禁时长」，
  触发成员在封禁期内无法使用 Bot 任何指令（到期自动解封，管理员不受限）；
  **触发后处置方式可选**：`自动封禁` 立即限制，或 `通知管理员处理`——发送待处理工单
  并 @管理员角色（工单频道优先，未设置则退回管理/审计日志频道），管理员可一键
  「立即封禁 / 放行」，**超过设置的处理时限仍未处理则自动封禁**；
  工单按钮重启后自动恢复，超时扫描持续运行，不会因 Bot 重启而漏处理
- 🚫 **管理员手动禁用**：`/ban_user` 直接禁用成员使用 Bot 的全部指令
  （可设时长，不填则永久），`/unban_user` 随时解除；禁用/解除均私信通知本人
  并写入管理日志，待处理风控工单会一并关闭避免重复处置
- 🎫 **申诉工单**：被风控成员 `/appeal` 提交申诉，Bot 发送到 `/ticket_channel`
  指定的频道并自动 @所有管理员角色，集齐足够管理员同意方可解封（默认 2 票，可用
  `/vote_config` 调整；同票数拒绝则驳回）；
  工单按钮重启后自动恢复，不会因 Bot 重启而卡死
- 🗳️ **投票人数可配置**：`/vote_config` 统一调整各类管理员投票所需同意人数
  （一键整理·频道 / 一键整理·子区 / 一键整理·全服 / 初始化服务器 / 申诉工单，范围 1~20，仅本服务器生效）
- 🔒 **下载密码**：上传时可选择设置密码（弹窗输入），下载该文件需输入正确密码，
  密码错误会记入管理日志
- ✏️ **上传重命名**：上传准备页可直接修改入库文件名（自动保留扩展名），
  改名记录同步到管理日志
- 🛡️ **管理日志**：`/storage_mode`、`/log_channel`、`/organize`、`/delete` 等
  管理员操作自动记录，可用 `/admin_log_channel` 单独设置管理日志频道
  （未设置时跟随审计日志频道）
- 🕵️ **下载审计**：下载必须经过 Bot 代理（`/download`），每次下载落库记录
  用户 ID、用户名、时间戳，并累计下载次数
- 📊 **审计能力**：管理员可查看任意文件的下载历史、任意成员的下载记录
- 🔍 **检索**：文件列表分页浏览、按名称/描述搜索
- 🗑️ **权限可控**：仅上传者或管理员可删除文件、查看下载历史
- 📝 **审计日志频道**：上传/下载事件自动推送到指定频道

## 📋 指令一览

| 指令 | 说明 | 权限 |
| --- | --- | --- |
| `/upload <附件> [描述]` | 上传文件（可设密码、重命名），返回文件 ID | 所有人 |
| `/download <文件ID>` | 下载文件（**会被记录**） | 所有人 |
| `/files [页码]` | 浏览文件列表 | 所有人 |
| `/search <关键词>` | 搜索文件 | 所有人 |
| `/fileinfo <文件ID>` | 查看文件详情 | 所有人 |
| `/history <文件ID>` | 查看该文件的下载记录 | 上传者 / 管理员 |
| `/delete <文件ID>` | 删除文件 | 上传者 / 管理员 |
| `/storage_mode` | 设置存储模式（子区 / 独立服务器） | 管理员 |
| `/log_channel [频道]` | 设置审计日志频道 | 管理员 |
| `/admin_log_channel [频道]` | 单独设置管理日志频道 | 管理员 |
| `/reset_server` | 初始化本服务器（默认 3 名管理员同意，可用 `/vote_config` 调整） | 管理员 |
| `/audit_user <成员>` | 查看某成员的下载记录 | 管理员 |
| `/stats` | 仓库统计 | 管理员 |
| `/organize <范围>` | 预扫描 + 投票整理存储文件（可选当前频道 / 当前子区 / 整个服务器），通过后确认黑白名单与是否登记散落文件 | 管理员 |
| `/organize_cancel` | 强制取消正在执行的整理/初始化任务 | 管理员 |
| `/vote_config` | 配置各类投票所需的管理员同意人数（整理 / 初始化 / 申诉） | 管理员 |
| `/risk_config` | 配置下载风控（频率上限与封禁时长） | 管理员 |
| `/ban_user <成员> [时长] [原因]` | 直接禁用成员使用 Bot（时长不填=永久） | 管理员 |
| `/unban_user <成员>` | 解除成员的 Bot 使用禁用 | 管理员 |
| `/ticket_channel <频道>` | 设置风控申诉工单发送频道 | 管理员 |
| `/appeal [理由]` | 被风控成员提交解封申诉工单 | 所有人 |

## 🚀 部署

### 1. 创建 Discord Bot

1. 前往 [Discord Developer Portal](https://discord.com/developers/applications) 创建应用
2. 在 **Bot** 页面复制 Token
3. 在 **OAuth2 → URL Generator** 勾选 `bot` 和 `applications.commands`，
   Bot 权限至少勾选：`Manage Channels`、`Send Messages`、`Attach Files`、`Read Message History`
4. 用生成的链接邀请 Bot 进服务器（若使用独立存储服务器模式，也要邀请进存储服务器）

### 2. 配置

```bash
git clone https://github.com/Hum404/discord-repo-bot.git
cd discord-repo-bot
cp .env.example .env
# 编辑 .env，填入 DISCORD_TOKEN
```

`.env` 配置项：

| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `DISCORD_TOKEN` | ✅ | Bot Token |
| `STORAGE_GUILD_ID` | 独立服务器模式必填 | 存储服务器的 Guild ID |
| `DATABASE_PATH` | 否 | SQLite 数据库路径，默认 `data/repository.db` |
| `MAX_FILE_SIZE_MB` | 否 | 单文件大小上限（MB），默认 24 |

### 3. 运行

```bash
pip install -r requirements.txt
python run.py
```

Docker（可选）：

```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "run.py"]
```

## 📄 日志查看

Bot 有三层日志，按用途选用：

1. **运行日志（排障用）**：启动信息、异常堆栈、整理失败明细等
   - 前台运行时直接输出在**终端控制台**
   - 同时写入文件 **`logs/bot.log`**（自动轮转：单文件 2MB，保留 5 份；
     可用环境变量 `LOG_DIR` 修改目录），启动时控制台会打印其完整路径
   - systemd 托管：`journalctl -u <服务名> -f`；Docker：`docker logs -f <容器名>`
2. **审计日志（服务器内）**：`/log_channel` 设置频道后，每次上传/下载实时推送
3. **管理日志（服务器内）**：`/admin_log_channel` 单独设置（未设置时跟随审计日志频道），
   记录 `/storage_mode`、`/organize`、`/vote_config`、`/delete`、风控处置等管理操作

## 🔍 下载审计说明

Discord 原生附件链接无法追踪访问者（且 CDN 链接会过期）。本 Bot 的做法是：

1. 上传时，文件被转存到 **Bot 控制的存储频道**，原始消息定位（频道 ID + 消息 ID）
   与元数据一起存入 SQLite
2. 下载时，用户必须通过 `/download <文件ID>` 向 Bot 索取，Bot 从存储频道取回文件并转发
3. 转发前先将 **文件 ID、用户 ID、用户名、服务器 ID、时间戳** 写入 `downloads` 表
4. 管理员/上传者可用 `/history` 和 `/audit_user` 随时审计；若配置了日志频道，
   每次上传/下载都会实时推送审计消息

> ⚠️ 注意：由于 Discord 平台限制，Bot 单文件发送上限受服务器加成等级影响
> （通常 25MB，加成等级 2 为 50MB，等级 3 为 100MB）。请按实际情况调整
> `MAX_FILE_SIZE_MB`。

## 🏗️ 项目结构

```
discord-repo-bot/
├── run.py               # 启动入口
├── requirements.txt
├── .env.example         # 配置模板
├── bot/
│   ├── bot.py           # Bot 主类、存储频道解析
│   ├── config.py        # 环境配置加载
│   ├── db.py            # SQLite 数据层（文件 / 下载记录 / 设置）
│   └── cogs/
│       ├── files.py     # 上传、下载、检索等指令
│       └── admin.py     # 管理员配置与审计指令
└── LICENSE
```

## 📝 更新日志

### v2.1.1

- 🐛 **修复投票人数调为 1 后投票不执行**：`/organize`、`/reset_server` 的发起者自动计 1 票，
  当所需人数为 1 时现在会立即通过并进入后续流程（此前会一直卡在等待投票）
- ✨ **`/organize` 新增「当前频道」范围**：只整理发起指令所在的文字频道
  （默认 1 票即可通过，可用 `/vote_config` 调整）
- ✨ **整理确认面板新增「散落文件登记」开关**：关闭后只迁移错位的已登记文件，
  不再把频道里成员随手发的图片/表情包登记入库
- ✨ **新增 `/organize_cancel`**：整理/初始化执行中可强制取消；存储频道在整理过程中
  被删除时会立即中止并释放任务锁，不再一直提示「已有整理/初始化任务在执行」

### v2.1.0

- 🐛 **修复 `/organize` 一键整理无法使用**：预扫描阶段变量引用错误，指令在
  「正在预扫描整理范围…」前即报错中断；另修复 `/reset_server` 彻底重置模式的同类问题
- ✨ **投票人数可配置**：新增 `/vote_config`——管理员可统一调整各类投票所需的
  同意人数（一键整理·子区 默认 2 / 一键整理·全服 默认 3 / 初始化服务器 默认 3 /
  申诉工单解封或驳回 默认 2），范围 1~20，按服务器独立保存；申诉工单按钮在
  Bot 重启恢复时也会读取最新配置
- 📝 **运行日志落盘**：日志除控制台外同时写入 `logs/bot.log`（自动轮转，可用
  `LOG_DIR` 环境变量改目录），README 新增「日志查看」一节说明三层日志的查看方式
- ♻️ 移除文件溯源标记相关功能，保留下载记录审计

### v2.0.0

- 🔐 内部安全机制重构与加固（实现细节不予公开）

### v1.10.0

- ✨ 角色卡兼容改进：下载 PNG / JSON 角色卡时不再破坏卡片数据，
  导入角色卡软件完全不受影响

### v1.9.0

- ✨ **管理员手动禁用**：新增 `/ban_user`——管理员可直接禁用任意成员使用 Bot
  全部指令，支持自定义时长（小时，不填或填 0 为永久）与原因；禁用后私信通知本人、
  写入管理日志，若该成员有待处理风控工单会一并关闭（避免超时扫描覆盖手动处置）；
  新增 `/unban_user` 随时解除禁用并私信告知。被禁用成员仍可使用 `/appeal` 申诉，
  到期自动解除；管理员不受禁用限制
- ✨ **工单处理结果私信通知完善**：
  - 风控待处理工单点击「放行」后也会私信通知用户（此前只有封禁才通知）
  - 申诉通过/驳回、手动封禁、超时自动封禁等所有工单结果的私信均带上服务器名称
  - 私信未送达（对方关闭私信）时，在管理员的指令回执中明确提示

### v1.8.0

- ✨ **风控处置方式可配置**：`/risk_config` 新增触发后处置选择——
  - `⛔ 自动封禁`：检测到异常立即封禁（原行为，默认）
  - `🚨 通知管理员处理`：生成待处理工单发送到管理员频道并 @所有管理员角色，
    管理员可点击「立即封禁」或「放行」；**超过设置的处理时限（分钟，可配置）
    仍未处理则自动封禁**并更新工单消息、私信通知被封禁成员
  - 工单频道优先，未设置时退回管理日志频道 → 审计日志频道；
    都未配置时自动退化为立即封禁，保证异常行为必被处置
  - 同一成员的待处理工单不重复创建；工单按钮重启后自动恢复，
    后台每 30 秒扫描超时工单，Bot 重启不漏处理

### v1.7.0

- ✨ **下载风控**：新增 `/risk_config`——管理员可开关风控并设置
  「统计窗口（分钟）/ 窗口内下载次数上限 / 封禁时长（小时，可填 0.5）」；
  成员短时多次下载触发封禁，期间无法使用 Bot 任何指令（`/appeal` 除外），
  到期自动解封；管理员不受限制；触发与解封均写入管理日志
- ✨ **申诉工单**：新增 `/appeal` 与 `/ticket_channel`——被风控成员可提交解封申诉，
  Bot 将工单发送到管理员指定频道并自动 @所有管理员角色；集齐 2 名管理员同意方可解封，
  2 票拒绝则驳回；结果私信通知申请人；工单按钮在 Bot 重启后自动恢复
- ♻️ **一键整理黑白名单并入投票后确认面板**：删除独立的 `/organize_filter` 指令；
  投票通过后直接在确认面板选择「黑名单 / 白名单」模式与名单频道（自动载入默认配置），
  确认即本次生效；「确认并保存为默认」一键写为默认配置，以后每次整理均可再修改

### v1.6.1

- 🐛 新增全局指令错误处理：指令异常时回复明确提示并记录完整堆栈到日志，
  不再出现无声无息的「应用程序未响应」
- 🐛 `/organize_filter` 增加 discord.py 版本检测，版本过低时给出升级指引
- 🐛 指令同步失败改为捕获全部异常，启动更稳健

### v1.6.0

- ✨ **一键整理黑白名单**：
  - `/organize` 现在先预扫描再投票，投票嵌入中展示待迁移文件数与各频道散落文件数
  - 投票通过后进入**范围确认面板**：可按频道勾选本次跳过的对象，
    「🚫 排除并永久拉黑」一键写入持久名单
  - 新增 `/organize_filter`：配置永久生效的黑名单模式（整理名单以外全部）/
    白名单模式（只整理名单内），闲聊、公告等频道不再被误扫
  - 整理执行时以最新配置为准（投票后到确认前改的配置也会生效）

### v1.5.2

- 🐛 修复附件读取/存储频道操作仅捕获 `HTTPException` 的问题：Discord CDN 网络异常
  （超时、连接重置）时异常未被捕获，导致指令永久卡在「正在响应」。现统一增加
  超时控制与全类型异常捕获，失败时明确提示
- 📝 新增 [服务条款](TERMS_OF_SERVICE.md) 与 [隐私政策](PRIVACY_POLICY.md)，
  可复制其 URL 配置到 Discord 开发者后台的 Bot 简介

### v1.5.1

- 🐛 修复大文件下载时的一系列稳定性问题：处理改为线程执行，不再阻塞事件循环；
  超大文件增加输出守卫，保证文件始终能送达

### v1.5.0

- ✨ 上传准备页改进：确认上传前可调整下载相关选项

### v1.4.0

- ✨ 新增 `/reset_server` 服务器初始化：需 3 名管理员投票同意（发起者自动计 1 票），
  通过后清空全部文件记录、下载记录与服务器设置，Bot 恢复到刚加入时的状态
- 两种模式：仅清空数据（保留存储频道，之后可用 `/organize` 重新登记入库）/
  彻底重置（连同存储频道及其中所有文件一起删除，自动创建的专用分类清空后一并删除）
- 初始化前后均写入管理日志；与整理功能共用执行锁，避免并发冲突

### v1.3.0

- ✨ 下载审计日志增强，记录每一次下载行为

### v1.2.0

- 🐛 修复/完善：`/organize` 整理不到文件的问题——原逻辑只迁移「已登记但不在
  存储频道」的文件，正常上传的文件本来就在存储频道，因此永远显示已归拢 0 个。
  现在新增**散落文件扫描**：范围内频道中未登记的文件消息会自动登记入库并归拢到
  存储频道（本就在存储频道的单附件消息原地登记，多附件消息拆分入库）
- ✨ 上传流程改为准备页：`/upload` 后可选择**设置下载密码**（弹窗输入）、
  **重命名文件**（自动保留扩展名），确认后再写入存储频道
- ✨ 下载密码保护：加密文件 `/download` 时弹窗验证密码，密码错误记入管理日志
- ✨ 新增管理日志：`/storage_mode`、`/log_channel`、`/admin_log_channel`、
  `/audit_user`、`/organize`、管理员删除文件、上传重命名等操作自动记录；
  新增 `/admin_log_channel` 指令单独设置管理日志频道（未设置时跟随审计日志频道）
- ✨ `/fileinfo` 增加密码保护标识

### v1.1.0

- 🐛 修复：`/storage_mode` 存储服务器 ID 增加格式校验与明确报错（ID 需为纯数字；
  Bot 未加入目标服务器时会明确提示，不再静默失败）
- ✨ `/storage_mode` 子区模式新增 `category` 参数：可直接使用已建好的子区，
  不填则保持原行为（自动新建专用子区）
- ✨ 新增 `/organize` 一键整理：管理员投票制（当前子区 2 票、整个服务器 3 票），
  自动迁移散落文件、保留原入库卡片并同步更新索引
- ✨ 文件自动编号：上传回执、`/files`、`/search`、`/fileinfo` 均显示编号，
  相关指令可直接用编号代替文件 ID
- 🔧 移除不必要的 Privileged Intents 申请（避免未开启时启动报错）

## 📄 法律文档

- [服务条款（Terms of Service）](TERMS_OF_SERVICE.md)
- [隐私政策（Privacy Policy）](PRIVACY_POLICY.md)

## 📄 许可证

MIT License
